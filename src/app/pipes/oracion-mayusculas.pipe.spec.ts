import { OracionMayusculasPipe } from './oracion-mayusculas.pipe';

describe('OracionMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new OracionMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
